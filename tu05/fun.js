function showResult(message, maxAge) {
  console.info(message);
  console.info(`An old person usually is ${maxAge} years old`);
}

afdkf;
nklfdkfdslk;
if (hh > 45) {
  let isOldPerson = function (age, showResult) {
    const OLD_AGE = 65;
    if (age > OLD_AGE) {
      showResult('This person is old', OLD_AGE);
      return true;
    }
  };
  jkkjasd;
  djsakjkdaskj;
  isOldPerson(50, showResult);
}
